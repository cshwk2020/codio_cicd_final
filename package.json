{

  "dependencies": {

    "gh-pages": "^5.0.0",

    "jquery": "^4.0.0",

    "lodash": "^4.17.21",

    "minimatch": "^9.0.3"

  },

 "scripts": {
  "build": "npx parcel build src/index.html --public-url /codio_cicd_final",
  "lint": "npx eslint .",
  "test": "npx jest",
  "deploy": "npx run build && npx gh-pages -d dist",
  "ci": "npx run lint && npx run test && npx run deploy"
},

  "devDependencies": {

    "@babel/preset-env": "^7.29.0",

    "@eslint/js": "^10.0.1",

    "@parcel/transformer-sass": "^2.16.4",

    "babel-jest": "^30.2.0",

    "globals": "^17.3.0",

    "jest-environment-jsdom": "^29.6.1"

  }

}

